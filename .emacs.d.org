:PROPERTIES:
:ID:       193B271C-CB97-4FDA-A372-28D4DDFD61E8
:END:
#+title: ~/.emacs.d
#+author: Jeff Kreeftmeijer
#+date: 2021-07-04
#+options: toc:t

* General configuration
:PROPERTIES:
:CUSTOM_ID: general
:END:

#+include: "emacs-general-configuration.org" :lines "3-"

#+headers: :tangle early-init.el
#+headers: :exports none
#+begin_src emacs-lisp
  (load "~/.emacs.d/general.el" nil t)
#+end_src

* Package management
:PROPERTIES:
:CUSTOM_ID: package-management
:END:

#+include: "emacs-straight-use-package.org" :lines "10-"

#+headers: :tangle init.el
#+headers: :exports none
#+begin_src emacs-lisp
  (load "~/.emacs.d/package-management.el" nil t)
#+end_src

#+headers: :tangle early-init.el
#+headers: :exports none
#+begin_src emacs-lisp
  (load "~/.emacs.d/early-package-management.el" nil t)
#+end_src

* Linguist
:PROPERTIES:
:CUSTOM_ID: linguist
:END:

#+include: "emacs-linguist.org" :lines "10-"

#+headers: :tangle init.el
#+headers: :exports none
#+begin_src emacs-lisp
  (load "~/.emacs.d/linguist.el" nil t)
#+end_src

* Evil
:PROPERTIES:
:CUSTOM_ID: evil
:END:

#+include: "emacs-evil.org" :lines "3-"

#+headers: :tangle init.el
#+headers: :exports none
#+begin_src emacs-lisp
  (load "~/.emacs.d/evil.el" nil t)
#+end_src

* Org mode
:PROPERTIES:
:CUSTOM_ID: org-mode
:END:

#+include: "emacs-org-mode.org" :lines "3-"

#+headers: :tangle init.el
#+headers: :exports none
#+begin_src emacs-lisp
  (load "~/.emacs.d/org.el" nil t)
#+end_src

* Org-roam
:PROPERTIES:
:CUSTOM_ID: org-roam
:END:

#+include: "emacs-org-roam.org" :lines "3-"

#+headers: :tangle init.el
#+headers: :exports none
#+begin_src emacs-lisp
  (load "~/.emacs.d/org-roam.el" nil t)
#+end_src

* Clipboard

Use the macOS system clipboard through =pbpaste=/=pbcopy= by using [[https://elpa.gnu.org/packages/xclip.html][xclip]]:

#+headers: :tangle init.el
#+begin_src emacs-lisp
  (use-package xclip
    :init
    (xclip-mode))
#+end_src
